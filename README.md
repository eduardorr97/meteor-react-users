## Meteor + React Example Project

### Prerequisites
- Have Meteor installed (https://www.meteor.com/install)

### Running project

- Clone the project
- `cd` into the project folder
- Run in the terminal `meteor --settings settings.json`